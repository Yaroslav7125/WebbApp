## Build Setup

```bash
# install dependencies
$ npm install

$ npm run migrate # for running migrations

$ npm run seeds # for running seeds

# serve with hot reload at localhost:3001
$ npm run dev
```
